[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn src.focus_guard.server:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
